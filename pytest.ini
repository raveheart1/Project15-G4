[pytest]
testpaths = elephantcallscounter/tests
addopts = -m "not integration"
markers =
    unit: isolated tests that do not require cloud credentials, audio codecs, or trained models
    integration: tests that require external services, large audio files, or trained model artifacts
