# Dependency definitions are canonical in pyproject.toml.
# This installs the full runtime: core + audio pipeline + optional CNN + cloud.
# Equivalent to: pip install ".[audio,ml,cloud]"
.[audio,ml,cloud]
