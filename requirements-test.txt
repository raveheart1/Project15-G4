# Dependency definitions are canonical in pyproject.toml.
# This installs the package (editable) with the lint/test extra only -- the
# core stack, no cloud/audio/ML. Equivalent to: pip install -e ".[test]"
-e .[test]
